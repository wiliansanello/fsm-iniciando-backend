<%- include ('partials/header') %>
  <p><%= time %></p>
  <ul>
  <% lista.forEach((item) => { %>
    <li><%= item %></li>
  <% }) %>
</ul>
<ul>
  <% animais.forEach((item) => { %>
    <li>Name: <%= item.nome %></li>
  <% }) %>
</ul>
<%- include('partials/footer') %>